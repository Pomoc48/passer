import { FirebaseApp } from "firebase/app";

function Login(params: { app: FirebaseApp }) {
    return (
        <div className="Login">
            <h2></h2>
        </div>
    );
}

export default Login;
